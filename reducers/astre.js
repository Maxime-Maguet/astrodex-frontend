import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const astreSlice = createSlice({
  name: "astre",
  initialState,
  reducers: {},
});

export const {} = astreSlice.actions;
export default astreSlice.reducer;
